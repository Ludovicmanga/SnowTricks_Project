{% extends 'base.html.twig' %}

{% block body %}
    <h1 id="registration_header">Inscription sur le site</h1>

    {{ form_start(form, {'attr' : {'class' : 'registration_form'}}) }}
        {{ form_row(form.userName, {'attr' : {'placeholder' : 'Nom d\'utilisateur'}}) }}
        {{ form_row(form.email, {'attr' : {'placeholder' : 'Adresse e-mail'}}) }}
        {{ form_row(form.password, {'attr' : {'placeholder' : 'Mot de passe'}}) }}
        {{ form_row(form.confirm_password, {'attr' : {'placeholder' : 'Confirmer votre mot de passe'}}) }}
        <label>Sélectionnez votre photo de profil</label>
        {{ form_row(form.profile_picture) }}
        <div id="registration_button_container">
            <button type="submit">S'inscrire</button>
        </div>
    {{ form_end(form) }}
{% endblock %}